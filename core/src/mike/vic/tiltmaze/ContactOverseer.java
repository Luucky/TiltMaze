package mike.vic.tiltmaze;

import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g3d.attributes.ColorAttribute;
import com.badlogic.gdx.physics.bullet.collision.ContactListener;
import com.badlogic.gdx.physics.bullet.collision.btCollisionObject;

/**
 * Created by Mike on 2016-03-03.
 */
class ContactOverseer extends ContactListener {
//    @Override
//    public boolean onContactAdded(btCollisionObject p0, int partId0, int index0, boolean match0, btCollisionObject p1, int partId1, int index1, boolean match1) {
//        if (match0) {
//            if (p0.getUserValue() == 2)
//                ColorAttribute)maze.materials.get(0).get(ColorAttribute.Diffuse)).color.set(Color.WHITE
//
//        }
//        if (match1) {
//            if (p1.getUserValue() == 0)
//                ;//contactMatrix = p1.getWorldTransform();
//            else ((ColorAttribute)instances.get(p1.getUserValue()).materials.get(0).get(ColorAttribute.Diffuse)).color.set(Color.WHITE);
//        }
//        return true;
//    }
}